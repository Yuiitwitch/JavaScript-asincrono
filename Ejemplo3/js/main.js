const cambiarPantalla = (valorDestino) => {

//primer paso, generamos array con todas las fases
let arrayDeFases = ["1","2","3","4"];
//Filtramos el array que contiene las fases, le quitamos el calor que corresponde
//a la pantalla de destino.
arrayDeFases = arrayDeFases.filter(iterador => !pantallaDestino.includes(iterador));

    document.getElementById(pantallaDestino)
    for (let cadaPosicion ) {

    }
}